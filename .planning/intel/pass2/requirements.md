# Requirements (PRD intel — Pass 2)

No PRD-classified documents in the Pass 2 source set. The 35 ingested docs are all
ADRs (31 active LOCKED, 4 Superseded).

No requirements extracted. No competing acceptance-criteria variants (requires ≥2
PRDs overlapping on the same scope; none present).

Requirements for this ingest are derived downstream by `gsd-roadmapper` from the
LOCKED decisions in `decisions.md`, combined with existing Pass 1
`.planning/REQUIREMENTS.md`.
