# Requirements (PRD intel — Pass 3)

No PRD-classified documents in the Pass 3 source set (0 PRD of 44 docs: 1 ADR, 20 SPEC,
23 DOC). No requirements extracted and therefore no competing acceptance variants.

Product/requirement intent for these features is already captured in
`.planning/REQUIREMENTS.md` (Pass 1 + Pass 2). The Pass 3 SPEC/DOC set supplies
implementation contracts (see `constraints.md`) and background runbooks (see `context.md`)
that elaborate the existing requirements and locked decisions — not new requirements.
