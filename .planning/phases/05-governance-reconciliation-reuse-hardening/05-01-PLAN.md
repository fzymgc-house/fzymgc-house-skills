---
phase: 05-governance-reconciliation-reuse-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/adr/README.md
  - docs/adr/fhsk-dgo-use-release-please-file-plugin-versions-reverse-cog-tag-only.md
  - docs/adr/
  - .beads/
autonomous: true
requirements: [GOV-01]
tags: [adr, governance, release-please, supersession, layout]

must_haves:
  truths:
    - "docs/adr/README.md index shows fhsk-dgo status as 'Superseded by <fhsk-AAA>' (D-05)"
    - "New ADR fhsk-AAA carries 'Supersedes: fhsk-dgo' and its Decision lists all SIX version-synced manifests, adding tmux/plugin.json + grepping/plugin.json to fhsk-dgo's stale four (D-02, D-03)"
    - "New ADR fhsk-BBB records the shipped 5-plugin root layout (homelab/jj/dev-flow/tmux/grepping), marks the fzymgc-house/skills/* per-skill package layout superseded-in-practice in prose, and records PR-review landing inside dev-flow (D-04)"
    - "fhsk-BBB References section lists 'Related: fhsk-AAA' and 'Related: fhsk-a6v' (body bullets, NOT supersedes edges) (D-04)"
    - "fhsk-AAA does NOT re-declare Supersedes for fhsk-toy or fhsk-7y4 (they remain superseded by fhsk-dgo only) (D-05)"
    - "task lint passes: adr-doctor reports no INV-A22 drift (committed markdown == render-adr output) and INV-A25 title frontmatter present on both new ADRs (D-06)"
  artifacts:
    - "docs/adr/<fhsk-AAA>-<slug>.md (release-please versioning ADR, rendered)"
    - "docs/adr/<fhsk-BBB>-<slug>.md (shipped-layout ADR, rendered)"
    - "docs/adr/fhsk-dgo-*.md re-rendered with Status flipped to Superseded"
    - "docs/adr/README.md index regenerated (two ADRs added, fhsk-dgo flipped)"
    - "Two bd decision beads (type=decision, closed=Accepted) with a supersedes dep edge <fhsk-AAA> -> fhsk-dgo"
  key_links:
    - "bd supersedes dep edge <fhsk-AAA> -> fhsk-dgo drives BOTH the new ADR's 'Supersedes' line and fhsk-dgo's 'Superseded by' line and the index status (render-adr computes status from the edge)"
    - "render-adr output must equal every committed ADR file (adr-doctor INV-A22); index sentinels must bracket the regenerated table (INV-A12)"
---

<objective>
Reconcile the release-please governance record with shipped reality by authoring TWO new ADRs
via the repo's bd + render-adr machinery, and flipping the superseded predecessor's status.

- fhsk-AAA re-homes the LOCKED release-please decision (manifest mode, one repo-wide version,
  automated CHANGELOG + manifest sync) forward INTACT while correcting its one factual error: the
  synced-manifest list grew from four to six (added tmux/plugin.json + grepping/plugin.json).
  "Superseded" here means re-homed into a corrected record, NOT reversed (D-02, D-03).
- fhsk-BBB records the shipped 5-plugin root layout and marks the design-plan's
  fzymgc-house/skills/* per-skill package layout superseded-in-practice (prose, since the design
  plan is not a bd bead), Related to fhsk-AAA and fhsk-a6v (D-04).

Purpose: GOV-01 — a superseding ADR records the shipped layout and marks the release-please ADR's
package layout superseded, closing the last governance drift thread of the marketplace project.
Output: two rendered ADR files, a re-rendered fhsk-dgo (Superseded), a regenerated ADR index, and
two bd decision beads joined by one supersedes edge.

fhsk-AAA and fhsk-BBB are PLACEHOLDER ids — bd assigns the real 4-char ids at creation. Substitute
the returned ids everywhere ($AAA / $BBB below).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-governance-reconciliation-reuse-hardening/05-CONTEXT.md
@docs/adr/fhsk-dgo-use-release-please-file-plugin-versions-reverse-cog-tag-only.md
@docs/adr/fhsk-a6v-add-tmux-as-standalone-plugin-rather-than-folding-into-dev-f.md
@docs/adr/README.md
@release-please-config.json
@dev-flow/skills/evolve-adr/SKILL.md
@dev-flow/skills/capture-adrs/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fhsk-AAA (release-please versioning ADR) and supersede fhsk-dgo</name>
  <files>docs/adr/&lt;$AAA&gt;-&lt;slug&gt;.md, docs/adr/fhsk-dgo-use-release-please-file-plugin-versions-reverse-cog-tag-only.md, .beads/</files>
  <read_first>
    - docs/adr/fhsk-dgo-use-release-please-file-plugin-versions-reverse-cog-tag-only.md (the ADR being superseded — carry its decision forward intact; note it lists only FOUR manifests)
    - release-please-config.json (the authoritative extra-files list: SIX json paths)
    - dev-flow/skills/evolve-adr/SKILL.md sections "When to Use Each Entry Point" (/adr new), "Supersession Semantics", "Status Mechanics"
    - dev-flow/skills/capture-adrs/SKILL.md step 7 "Write phase" (the quoted-heredoc bd create --type decision body pattern and the literal-backslash-n guard)
  </read_first>
  <action>
    Author fhsk-AAA using the DIRECT bd + render-adr path (the retrospective /adr new + /adr supersede
    semantics documented in evolve-adr), NOT the interactive capture-adrs approval loop — the executor
    has no main-session AskUserQuestion (INV-A19), so the approval loop is unavailable and unneeded here.

    Create the decision bead with `bd create --type decision --label phase:design --metadata` setting
    the `adr_deciders` metadata key to "Sean Brandt" (match existing ADRs), and pass the 5-section body via
    a QUOTED heredoc delimiter so the shell does not expand it; write bulleted lines as REAL line breaks,
    never the literal two-character escape backslash-n (the repo test
    tests/test_adr_docs.py::test_no_literal_newline_escapes_in_adrs and the capture-adrs guard both reject
    literal escapes). The body MUST contain these headings (adr-doctor INV-A20/A21/A4):
    `## Context`, `## Decision`, `## Rationale`, `## Alternatives Considered`, `## Consequences`.
    - `## Context`: fhsk-dgo (Accepted, LOCKED, correct in intent) recorded the release-please reversal,
      but its Decision prose enumerates only FOUR version-synced manifests; two plugins (tmux, grepping)
      were added afterward and their plugin.json version entries are now in the live sync list. Re-home the
      decision into a corrected record rather than leave stale prose. State explicitly this is a re-home,
      not a reversal — release-please (manifest mode, one repo-wide version) stays fully in force.
    - `## Decision`: restate the release-please decision intact (push-triggered on main; the org-wide
      RELEASE_PLEASE_APP_ID / RELEASE_PLEASE_PRIVATE_KEY App as protect-main bypass actor; one repo-wide
      version in .release-please-manifest.json; automated CHANGELOG.md; cog removed; PR-title validation via
      amannn/action-semantic-pull-request) AND correct the synced-manifest list to the CURRENT SIX
      release-please-config.json extra-files jsonpaths: `.claude-plugin/marketplace.json`,
      `homelab/plugin.json`, `jj/plugin.json`, `dev-flow/plugin.json`, `tmux/plugin.json`,
      `grepping/plugin.json` (the last two are the D-03 delta). Note that `.agents/plugins/marketplace.json`
      is deliberately NOT in extra-files (Codex version is not auto-synced — recorded, not changed here).
    - `## Rationale`: the four-to-six drift is the substantive justification for superseding rather than
      leaving fhsk-dgo as-is; carrying the decision forward keeps one authoritative, accurate record.
    - `## Alternatives Considered`: leave fhsk-dgo untouched and note the drift elsewhere (rejected — stale
      canonical record); evolve fhsk-dgo in place (rejected — user chose formal supersession, D-01).
    - `## Consequences`: fhsk-dgo flips to Superseded; the corrected six-manifest list is now canonical; no
      behavior change to the release pipeline.
    Do NOT hand-write a `## References` section for the Supersedes line — the supersedes dep edge produces it.

    Capture the returned id as $AAA; guard it is non-empty and non-null (abort on failure). Close the bead
    immediately (`bd close $AAA --reason "Accepted ADR filed via /adr new"`) so it renders as Accepted. Then
    create the supersession edge and re-render BOTH files: `bd dep add $AAA fhsk-dgo --type=supersedes`,
    `bd close fhsk-dgo --reason "Superseded by $AAA"`, then run render-adr (via
    `${CLAUDE_PLUGIN_ROOT}/scripts/render-adr`, falling back to `dev-flow/scripts/render-adr`) for BOTH $AAA
    and fhsk-dgo from the repo root. render-adr derives the slug and writes docs/adr/&lt;id&gt;-&lt;slug&gt;.md.
    Add ONLY the single fhsk-dgo edge — do NOT re-declare Supersedes for fhsk-toy/fhsk-7y4 on fhsk-AAA (they
    stay under fhsk-dgo only, D-05).
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/fzymgc-house/fzymgc-house-skills && AAA=$(bd dep list fhsk-dgo --direction=up --type=supersedes --json 2>/dev/null | jq -r '.[0].id') && test -n "$AAA" && rg -q "Superseded by ${AAA}" docs/adr/fhsk-dgo-*.md && rg -q "Supersedes: fhsk-dgo" docs/adr/${AAA}-*.md && rg -q "grepping/plugin.json" docs/adr/${AAA}-*.md && rg -q "tmux/plugin.json" docs/adr/${AAA}-*.md</automated>
  </verify>
  <acceptance_criteria>
    - `bd dep list fhsk-dgo --direction=up --type=supersedes` returns exactly one edge whose id is the new fhsk-AAA bead.
    - docs/adr/fhsk-dgo-*.md `**Status:**` line reads `Superseded by <fhsk-AAA>` (re-rendered, not hand-edited).
    - docs/adr/<fhsk-AAA>-<slug>.md exists, opens with YAML `title:` frontmatter (no body H1), and its `## References` section (produced by render) contains `- Supersedes: fhsk-dgo` and NO line for fhsk-toy or fhsk-7y4.
    - docs/adr/<fhsk-AAA>-<slug>.md `## Decision` names all six of: `.claude-plugin/marketplace.json`, `homelab/plugin.json`, `jj/plugin.json`, `dev-flow/plugin.json`, `tmux/plugin.json`, `grepping/plugin.json`.
    - `dev-flow/scripts/adr-doctor` reports no INV-A22 drift for fhsk-AAA or fhsk-dgo (committed == render).
  </acceptance_criteria>
  <done>fhsk-AAA exists as a closed decision bead + rendered ADR that supersedes ONLY fhsk-dgo and lists the corrected six synced manifests; fhsk-dgo re-rendered to Superseded.</done>
</task>
<task type="auto">
  <name>Task 2: Author fhsk-BBB (shipped 5-plugin layout ADR, Related to fhsk-AAA + fhsk-a6v)</name>
  <files>docs/adr/&lt;$BBB&gt;-&lt;slug&gt;.md, .beads/</files>
  <read_first>
    - docs/adr/fhsk-a6v-add-tmux-as-standalone-plugin-rather-than-folding-into-dev-f.md (layout-precedent ADR — fhsk-BBB lists it as Related; mirror its section shape)
    - docs/plans/2026-02-16-release-please-design.md (the design PLAN carrying the fzymgc-house/skills/* per-skill package layout being marked superseded-in-practice)
    - docs/plans/2026-02-23-agent-plugin-restructure-design.md (records the restructure into the shipped homelab + true dev-flow layout — grounding for the prose)
    - .planning/codebase/STRUCTURE.md (authoritative shipped 5-plugin directory map: homelab/jj/dev-flow/tmux/grepping)
    - dev-flow/scripts/_adr_render.py split_body_references (confirms a `## References` section in the bd description body is preserved verbatim; "Related:" is authored as body bullets, NOT a dep edge)
  </read_first>
  <action>
    Author fhsk-BBB via the same direct `bd create --type decision` + render-adr path as Task 1 (quoted
    heredoc body, real newlines, `adr_deciders` = "Sean Brandt"). This ADR is STANDALONE — it adds NO
    supersedes dep edge (the fzymgc-house/skills/* layout lived only in a design PLAN, which is not a bd
    decision bead, so no formal supersession edge exists; the supersession is stated in prose per D-04).
    The body MUST contain `## Context`, `## Decision`, `## Rationale`, `## Alternatives Considered`,
    `## Consequences`, AND a trailing `## References` section.
    - `## Context`: the original single fzymgc-house plugin plus the release-please design plan's
      fzymgc-house/skills/* per-skill package layout were both superseded in practice by the shipped
      restructure; the governance record never captured the as-shipped layout.
    - `## Decision`: record the shipped 5-plugin ROOT layout — homelab, jj, dev-flow, tmux, grepping — each
      a top-level plugin dir (plugin.json + skills/), with Codex thin wrappers under plugins/<name>/ that
      symlink back to source (single source of truth). Explicitly mark the design-plan's
      fzymgc-house/skills/* per-skill package layout SUPERSEDED-IN-PRACTICE. Record that PR-review work
      landed INSIDE dev-flow (review-pr / address-findings / respond-to-comments skills + review/fix agents),
      NOT as a standalone pr-review plugin.
    - `## Rationale`: one-plugin-per-tool-domain taxonomy (consistent with fhsk-a6v); reuse without
      cross-plugin coupling; matches what actually shipped and what the marketplaces/CI already assert.
    - `## Alternatives Considered`: a single broad packaging-governance ADR folding versioning + layout
      (rejected — user chose split concerns, D-01/two-ADR split); a standalone pr-review plugin (rejected —
      PR-review shipped inside dev-flow).
    - `## References`: author these as literal body bullets (render-adr preserves the section verbatim):
      `- Related: fhsk-AAA` (substitute the real id from Task 1) and `- Related: fhsk-a6v`. Do NOT phrase
      either as "Supersedes" and do NOT add a supersedes dep edge.
    Close the bead immediately (Accepted) and run render-adr for $BBB from the repo root.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/fzymgc-house/fzymgc-house-skills && BBB=$(bd list --type decision --json 2>/dev/null | jq -r '[.[] | select((.title|test("layout|plugin";"i")) and (.id!="fhsk-a6v"))] | sort_by(.created_at) | last | .id') && test -n "$BBB" && rg -q "Related: fhsk-a6v" docs/adr/${BBB}-*.md && rg -q "grepping" docs/adr/${BBB}-*.md && rg -q "dev-flow" docs/adr/${BBB}-*.md && test -z "$(bd dep list ${BBB} --direction=down --type=supersedes --json 2>/dev/null | jq -r '.[].id')"</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/<fhsk-BBB>-<slug>.md exists, opens with YAML `title:` frontmatter (no body H1), and passes adr-doctor INV-A22 (no drift) / INV-A25 (title present).
    - `## Decision` names all five shipped plugin dirs (homelab, jj, dev-flow, tmux, grepping), marks fzymgc-house/skills/* superseded-in-practice, and states PR-review landed inside dev-flow.
    - `## References` lists `Related: <fhsk-AAA>` and `Related: fhsk-a6v`; the bead has ZERO outgoing supersedes edges (`bd dep list <BBB> --direction=down --type=supersedes` is empty).
  </acceptance_criteria>
  <done>fhsk-BBB exists as a closed decision bead + rendered ADR recording the shipped layout, superseding the design-plan layout in prose only, Related (not Supersedes) to fhsk-AAA and fhsk-a6v.</done>
</task>
<task type="auto">
  <name>Task 3: Regenerate the ADR index and gate the whole set through adr-doctor</name>
  <files>docs/adr/README.md</files>
  <read_first>
    - docs/adr/README.md (the index table between the BEGIN INDEX / END INDEX sentinels; current fhsk-dgo row reads Accepted and both new ADRs are absent)
    - dev-flow/skills/capture-adrs/SKILL.md step 8 "Regenerate docs/adr/README.md" (walk docs/adr/, parse Date/title/Status, sort by date desc, rewrite between sentinels)
    - dev-flow/scripts/_adr_doctor.py check_readme (INV-A12: README + both index sentinels present) and check_render_match (INV-A22)
  </read_first>
  <action>
    Regenerate the index table in docs/adr/README.md between the `<!-- BEGIN INDEX -->` and
    `<!-- END INDEX -->` sentinels by walking every non-README `<bd-id>-<slug>.md` in docs/adr/, parsing
    each file's `**Date:**`, title (from YAML `title:`), and `**Status:**`, and emitting rows sorted by date
    descending — exactly the capture-adrs step 8 algorithm. The regenerated table MUST include a row for
    fhsk-AAA, a row for fhsk-BBB, and MUST show fhsk-dgo as `Superseded by <fhsk-AAA>`. Preserve the
    surrounding README prose (Writing guidelines section) untouched.

    Then run the full repo lint gate `task lint` from the repo root and resolve any adr-doctor findings.
    Common ones to expect and fix: INV-A22 drift (re-run render-adr for the offending id — never hand-edit
    the rendered body), rumdl markdown findings on docs/adr/README.md (run `task fmt` then re-check).
    Do NOT commit — the execute-plan workflow owns commits.
  </action>
  <verify>
    <automated>cd /Volumes/Code/github.com/fzymgc-house/fzymgc-house-skills && AAA=$(bd dep list fhsk-dgo --direction=up --type=supersedes --json 2>/dev/null | jq -r '.[0].id') && rg -q "\[${AAA}\]" docs/adr/README.md && rg -q "Superseded by ${AAA}" docs/adr/README.md && task lint && uv run --with pytest --with pyyaml pytest tests/test_adr_docs.py -q --import-mode=importlib</automated>
  </verify>
  <acceptance_criteria>
    - docs/adr/README.md index table (between BEGIN/END INDEX sentinels) contains a linked row for fhsk-AAA and one for fhsk-BBB.
    - The fhsk-dgo index row Status column reads `Superseded by <fhsk-AAA>`.
    - `task lint` exits 0 (adr-doctor + rumdl + ruff + jq all clean); `tests/test_adr_docs.py` passes (sentinels intact, no literal backslash-n escapes in any ADR).
  </acceptance_criteria>
  <done>The ADR index reflects both new ADRs and the flipped fhsk-dgo, and the full lint gate is green.</done>
</task>

</tasks>
<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author -> docs/adr | ADR content is authored via bd + render-adr into repo-local markdown; no external/untrusted input crosses into the repo. |

No runtime attack surface: this plan produces static ADR markdown plus bd decision beads. The only
executable tooling (bd, render-adr, adr-doctor) already ships in the repo and runs read-mostly over
repo-local state. There is no network, auth, secret handling, or user-supplied input.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | docs/adr/*.md rendered output | low | mitigate | adr-doctor INV-A22 fails on any drift between bd state and committed markdown; INV-A25 enforces title frontmatter; gated by `task lint`. |
| T-05-02 | Information Disclosure | ADR prose | low | accept | ADRs record already-public repo governance (release-please, layout); no secrets. Rationale: nothing sensitive is added. |
| T-05-SC | Tampering | package installs (npm/pip/cargo) | low | accept | No new package installs — bd, uv, and the ADR scripts are pre-existing repo tooling. No legitimacy checkpoint required. |

</threat_model>

<verification>
- `task lint` exits 0 (adr-doctor: INV-A4/A12/A20/A21/A22/A24/A25 clean; rumdl on docs/adr/README.md clean).
- `uv run --with pytest --with pyyaml pytest tests/test_adr_docs.py -q --import-mode=importlib` passes.
- `bd dep list fhsk-dgo --direction=up --type=supersedes` shows exactly one edge (the fhsk-AAA bead).
- fhsk-BBB has zero outgoing supersedes edges (Related is prose, not an edge).
</verification>

<success_criteria>

- Two new rendered ADRs exist: fhsk-AAA (Supersedes fhsk-dgo; corrected six-manifest list) and fhsk-BBB (shipped layout; Related fhsk-AAA + fhsk-a6v; fzymgc-house/skills/* superseded-in-practice in prose).
- fhsk-dgo re-rendered to `Superseded by <fhsk-AAA>` and shown as such in the ADR index; fhsk-toy/fhsk-7y4 remain superseded by fhsk-dgo only (chain unbroken).
- docs/adr/README.md index regenerated with both new rows; all GOV-01 acceptance gates (adr-doctor, test_adr_docs.py) green.
</success_criteria>

<artifacts_produced>

## Artifacts this phase produces (Plan 05-01)

- **docs/adr/&lt;fhsk-AAA&gt;-&lt;slug&gt;.md** — new rendered ADR: release-please versioning, Supersedes fhsk-dgo, corrected six-manifest sync list.
- **docs/adr/&lt;fhsk-BBB&gt;-&lt;slug&gt;.md** — new rendered ADR: shipped 5-plugin root layout; Related fhsk-AAA + fhsk-a6v; PR-review-inside-dev-flow; fzymgc-house/skills/* superseded-in-practice.
- **docs/adr/fhsk-dgo-*.md** — re-rendered; Status flipped to `Superseded by <fhsk-AAA>`.
- **docs/adr/README.md** — index table regenerated (two new rows; fhsk-dgo flipped).
- **bd decision bead &lt;fhsk-AAA&gt;** (type=decision, closed=Accepted, adr_deciders="Sean Brandt") + supersedes dep edge &lt;fhsk-AAA&gt; -> fhsk-dgo.
- **bd decision bead &lt;fhsk-BBB&gt;** (type=decision, closed=Accepted, adr_deciders="Sean Brandt", no supersedes edges).
</artifacts_produced>

<output>
Create `.planning/phases/05-governance-reconciliation-reuse-hardening/05-01-SUMMARY.md` when done.
Record the bd-assigned real ids substituted for fhsk-AAA / fhsk-BBB.
</output>
